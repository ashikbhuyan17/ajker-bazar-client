import React from 'react';
import Header from '../Header/Header';

const Deals = () => {
    return (
        <div>
            <Header />
            <h2>this is deals page</h2>
        </div>
    );
};

export default Deals;